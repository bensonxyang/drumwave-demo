{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost (8080)",
      "url": "http://127.0.0.1:8080/index.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Serve index.html on 8080"
    }
  ]
}